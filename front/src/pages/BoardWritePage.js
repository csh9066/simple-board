import React from 'react';
import BoardForm from '../components/board/BoardForm';

const BoardWritePage = ({ history }) => {
	return <BoardForm history={history} />;
};
export default BoardWritePage;
